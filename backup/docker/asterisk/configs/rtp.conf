;
; RTP Configuration
;

[general]
rtpstart=10000
rtpend=10100

; Strict RTP protection (prevent RTP injection attacks)
; TEMPORARILY DISABLED for ExternalMedia testing
strictrtp=no
probation=4

; Enable ICE support (REQUIRED for WebRTC)
icesupport=yes
stunaddr=stun.l.google.com:19302
