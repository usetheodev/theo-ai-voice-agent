# Core Dependencies for Docker Build
# Note: Some dependencies require manual installation (pjsua2, kokoro-onnx)

# AI Models (install what's needed for your use case)
transformers>=4.35.0
torch>=2.1.0
torchaudio>=2.1.0
accelerate>=0.24.0
sentencepiece>=0.1.99

# ASR (Whisper)
openai-whisper>=20231117

# Audio processing
numpy>=1.24.0
scipy>=1.11.0
librosa>=0.10.0
soundfile>=0.12.0

# Async
aiofiles>=23.2.0

# Configuration
pyyaml>=6.0
python-dotenv>=1.0.0

# Monitoring
prometheus-client>=0.19.0

# Utils
click>=8.1.7
