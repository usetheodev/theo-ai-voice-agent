# =============================================================================
# AI Agent Configuration - Example
# =============================================================================
# Copie este arquivo para .env e configure as variáveis

# WebSocket Server
WS_HOST=0.0.0.0
WS_PORT=8765

# Logging
LOG_LEVEL=INFO

# =============================================================================
# ASR/STT (Automatic Speech Recognition)
# =============================================================================
# Provider: faster-whisper (recomendado), whisper, openai
ASR_PROVIDER=faster-whisper

# Modelo: tiny (mais rápido), base, small, medium, large-v3
# Recomendado para CPU: tiny ou base
# - tiny: ~1s latência, menor precisão
# - base: ~2s latência, boa precisão
# - small: ~4s latência, melhor precisão
ASR_MODEL=tiny

# Idioma: pt, en, es, fr, de, etc.
ASR_LANGUAGE=pt

# Tipo de computação: int8 (CPU otimizado), float16 (GPU), float32
ASR_COMPUTE_TYPE=int8

# Device: cpu, cuda, auto
ASR_DEVICE=cpu

# =============================================================================
# LLM (Large Language Model)
# =============================================================================
# Provider: anthropic, openai, mock
LLM_PROVIDER=anthropic

# Anthropic Claude
ANTHROPIC_API_KEY=sk-ant-...
LLM_MODEL=claude-3-haiku-20240307

# OpenAI GPT (alternativo)
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-...
# OPENAI_LLM_MODEL=gpt-3.5-turbo

# Configurações comuns
LLM_MAX_TOKENS=256
LLM_TEMPERATURE=0.7
LLM_TIMEOUT=15.0

# System prompt customizado (opcional)
# LLM_SYSTEM_PROMPT=Você é um assistente virtual...

# =============================================================================
# TTS (Text-to-Speech)
# =============================================================================
# Provider: kokoro (recomendado), gtts, openai, mock
TTS_PROVIDER=kokoro

# Kokoro - TTS Neural Local de Alta Qualidade
# Vozes disponíveis:
#   Português:
#     - pf_dora (feminino brasileiro)
#     - pm_alex (masculino)
#     - pm_santa (masculino)
#   Inglês Americano:
#     - af_bella, af_nicole, af_sarah, af_sky (feminino)
#     - am_adam, am_michael (masculino)
#   Inglês Britânico:
#     - bf_emma, bf_isabella (feminino)
#     - bm_george, bm_lewis (masculino)
TTS_VOICE=pf_dora

# Sample rate nativo (será convertido para 8kHz para telefonia)
TTS_SAMPLE_RATE=24000

# gTTS - Google TTS (fallback gratuito)
# TTS_PROVIDER=gtts
TTS_LANG=pt

# OpenAI TTS (alternativo - requer API key)
# TTS_PROVIDER=openai
# OPENAI_TTS_MODEL=tts-1
# OPENAI_TTS_VOICE=alloy

# =============================================================================
# VAD (Voice Activity Detection)
# =============================================================================
# Agressividade: 0-3
# 0 = menos agressivo (mais tolerante a ruído)
# 3 = mais agressivo (detecta silêncio mais rápido)
VAD_AGGRESSIVENESS=2

# Threshold de silêncio para fim de fala (ms)
# Menor = resposta mais rápida (pode cortar fala)
# Maior = aguarda mais antes de processar
SILENCE_THRESHOLD_MS=500

# =============================================================================
# Mensagens do Agente
# =============================================================================
AGENT_GREETING=Olá! Bem-vindo ao atendimento. Como posso ajudá-lo?
AGENT_ERROR=Desculpe, tive um problema ao processar sua mensagem.
AGENT_GOODBYE=Até logo! Tenha um bom dia!
