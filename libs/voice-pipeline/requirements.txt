# Voice Pipeline Framework - Core Requirements
# ==============================================
#
# O core do framework usa apenas a biblioteca padrão do Python 3.10+
# Nenhuma dependência externa é necessária para o funcionamento básico.
#
# Este arquivo lista dependências OPCIONAIS para recursos específicos.

# Core (sem dependências externas - apenas Python 3.10+ stdlib)
# - asyncio, dataclasses, typing, abc, enum, etc.

# ============================================
# DEPENDÊNCIAS OPCIONAIS POR FUNCIONALIDADE
# ============================================

# Para memory stores persistentes
# redis>=4.5.0           # Redis store (memory/stores/redis.py)
# aiosqlite>=0.19.0      # SQLite async store (memory/stores/sqlite.py)

# Para tracing/observabilidade
# opentelemetry-api>=1.20.0
# opentelemetry-sdk>=1.20.0

# Para providers de ASR/TTS/LLM (instale conforme necessário)
# openai>=1.0.0          # OpenAI Whisper, GPT, TTS
# anthropic>=0.18.0      # Claude
# ollama>=0.1.0          # Ollama local
# elevenlabs>=0.2.0      # ElevenLabs TTS
# deepgram-sdk>=3.0.0    # Deepgram ASR

# Para processamento de áudio
# numpy>=1.24.0          # Manipulação de arrays de áudio
# soundfile>=0.12.0      # Leitura/escrita de arquivos de áudio
# webrtcvad>=2.0.10      # Voice Activity Detection

# Para type checking
# mypy>=1.0.0
# types-redis>=4.5.0
